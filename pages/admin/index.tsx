import AddMediaForm from "@/components/Common/AddMediaForm";
import Layout from "@/components/Common/Layout";

const Home: React.FC = () => {
  return (
    <Layout>
      <AddMediaForm />
    </Layout>
  );
};

export default Home;
